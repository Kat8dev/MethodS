import styled from "styled-components";

export const HomeWrapper = styled.div`
    min-height: 100vh;  
    display: flex;
    gap: 2rem;
    @media(min-width: 35rem) {
        flex-direction: row;
        .some-info {
              
        }
        .article {
           
        }
        .event {
            
        }
    }
`